
import { AdminDashboard } from "@/components/AdminDashboard";
import { bankApiService } from "@/services/bankApiService"; // Ensure API service is initialized

const AdminRoute = () => {
  return <AdminDashboard />;
};

export default AdminRoute;
